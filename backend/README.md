# Distributed Chat Application (Backend)

Spring Boot WebSocket (STOMP) chat service.

## Run
```bash
mvn spring-boot:run
```
Server starts on `http://localhost:8080` and exposes a STOMP endpoint at `/ws-chat`.
